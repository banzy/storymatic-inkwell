# Local MongoDB database used by the application server.
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=storymatic

# Conversation engine: server-side credentials only, never VITE_ prefixed.
OPENAI_API_KEY=
STORYMATIC_MODEL=gpt-6-astra
